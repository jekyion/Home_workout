<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.home_wrokout">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Homewrokout">
        <activity android:name=".PlecyPozActivity"></activity>
        <activity android:name=".NogiPozAzctivity" />
        <activity android:name=".RamionaPozActivity" />
        <activity android:name=".KlataPozActivity" />
        <activity android:name=".Alarm_RecyclerView.Alarm_add_activity" />
        <activity android:name=".ExTimerActivity" />
        <activity android:name=".BrzuchPozActivity" />
        <activity android:name=".TreningiActivity" />
        <activity android:name=".Alarm_RecyclerView.Alarm_activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>